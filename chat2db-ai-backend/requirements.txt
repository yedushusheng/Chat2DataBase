# ==========================================
# Chat2DB-AI Python Dependencies
# ==========================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
sse-starlette==2.1.0

# --- Config & Env ---
python-dotenv==1.0.1
pyyaml==6.0.1
pydantic==2.7.4
pydantic-settings==2.3.4

# --- RAG / AI / Vector ---
chromadb==0.5.0
sentence-transformers==3.0.1
langchain==0.2.0
langchain-community==0.2.0
langchain-text-splitters==0.2.0

# --- Document Parsing ---
pypdf==4.2.0
python-docx==1.1.2
markdown==3.6
# unstructured 暂不支持 Python 3.14，文档解析由 pypdf + python-docx 覆盖

# --- External APIs ---
openai==1.33.0
anthropic==0.28.0
httpx==0.27.0

# --- Utils ---
# numpy / pandas 在 Python 3.14 上编译困难，暂不固定版本
numpy
# pandas  # 当前未使用，如需请后续单独安装

# --- Logging ---
structlog==24.2.0

# --- Database (Conversation persistence) ---
aiosqlite==0.20.0

# --- Testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
